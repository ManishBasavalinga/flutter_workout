name: flutter_workout
description: A new Flutter application.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  sqflite: ^1.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.7.2

flutter_icons:
  android: true
  ios: false
  image_path: "assets/dumbell.png"
  adaptive_icon_background: "#DADADA"
  adaptive_icon_foreground: "assets/dumbell.png"

flutter:
  uses-material-design: true
